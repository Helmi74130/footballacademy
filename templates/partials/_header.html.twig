<nav class="navbar navbar-expand-lg bg-success mb-5" data-bs-theme="dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}"><img height="50px" src="{{ asset("img/ballon.png") }}"> Foot Academy</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link active" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_terrain') }}">Terrain</a>
                </li>

                <li class="nav-item">
                    <a class="nav-link" href="#">Réservation</a>
                </li>
            </ul>
            <div class="d-flex">
                {% if not app.user %}
                    <a href="{{ path('app_login') }}" class="btn btn-light me-3">Connexion</a>
                    <a href="{{ path('app_register') }}" class="btn btn-secondary">Inscription</a>
                {% else %}
                    <a href="{{ path('app_logout') }}" class="btn btn-danger">Déconnexion</a>
                {% endif %}
            </div>
        </div>
    </div>
</nav>